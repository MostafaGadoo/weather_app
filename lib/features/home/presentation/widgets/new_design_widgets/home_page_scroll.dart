import 'package:flutter/material.dart';

class HomePageScroll extends StatelessWidget {
  const HomePageScroll({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
